{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf dist/",
    "zipLambdas": "npm run clean && tsc && zip -r scripts/lambdas.zip dist",
    "zipDeps": "rm -rf nodejs/node_modules && cp -RL ../../node_modules nodejs/ && zip -r scripts/lambdasDependencies.zip nodejs",
    "zip": "npm run zipLambdas && npm run zipDeps",
    "update": "npm run zip && ./scripts/uploadLambdas.sh && ./scripts/updateLayers.sh"
  },
  "keywords": [],
  "author": "Conner Vigil",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "aws-sdk": "^2.1610.0",
    "typescript": "^5.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.12.7"
  }
}
